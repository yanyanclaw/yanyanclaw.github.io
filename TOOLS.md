# TOOLS.md - Local Notes

---

## Stock Price

**查股價唯一方法：exec 執行以下指令，直接貼輸出。**

```
python3 /root/stock-price.py <代號>
```

範例：
- 台積電：`python3 /root/stock-price.py 2330`
- 美股：`python3 /root/stock-price.py AAPL`
- 多支：`python3 /root/stock-price.py 2330 AAPL NVDA`

**嚴格禁止：**
- 禁止自己寫任何 Python 程式碼（yfinance、requests 等）
- 禁止在回覆裡顯示程式碼
- 禁止猜測或捏造股價數字
- 只能用上面的指令，把輸出直接回傳

---

## System Status

**查系統狀態唯一方法：exec 執行以下指令，直接貼輸出。**

```
bash /root/monitor.sh
```

**嚴格禁止：**
- 禁止說「我無法查詢系統狀態」
- 禁止捏造狀態資訊
- 只能用上面的指令，把輸出直接回傳

觸發詞：「狀態」「你還好嗎」「機器」「網路」「ollama」

---
